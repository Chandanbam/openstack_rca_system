#!/bin/bash

# Load environment variables from .env file
dotenv

# Docker Registry Configuration
export DOCKER_USERNAME=${DOCKER_USERNAME:-chandantech}
export DOCKER_PASSWORD=${DOCKER_PASSWORD:-}
export DOCKER_REGISTRY=${DOCKER_REGISTRY:-docker.io}
export DOCKER_REPOSITORY=${DOCKER_REPOSITORY:-openstack-rca-system}
 
# Optional: Show loaded environment status
echo "🔋 Environment loaded for OpenStack RCA System"
echo "✅ MLflow tracking: $MLFLOW_TRACKING_URI"
echo "✅ AWS region: $AWS_DEFAULT_REGION"
echo "✅ S3 endpoint: $MLFLOW_S3_ENDPOINT_URL"
echo "✅ Docker username: $DOCKER_USERNAME" 